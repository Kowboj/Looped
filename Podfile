# Configuration

PROJECT = 'Looped'
TARGET = 'Looped'

platform :ios, 10.0
install! 'cocoapods', integrate_targets: false

target TARGET do project PROJECT end

# Keys

plugin 'cocoapods-keys', {
  project: PROJECT,
  keys: [
    'CLIENT_ID',
    'CLIENT_SECRET'
  ]
}

